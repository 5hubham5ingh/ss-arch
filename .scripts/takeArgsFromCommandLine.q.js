#!/usr/local/bin/qjs

console.log("hi")
// test.js

// Access command line arguments
const args = scriptArgs.slice(1); // First element is the script file, so we slice it out

// Print the arguments
console.log('Arguments passed:', args);

